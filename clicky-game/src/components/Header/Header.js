import React from "react";
import "./Header.css";

const Header = props => (
    <header className = "header">
    <h1>Clicky Game!</h1>
    <h2> Click an image to earn points. Click the same image more than once and you lose!</h2>
    </header>
);

export default Header;